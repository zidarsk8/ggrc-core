{{!
  Copyright (C) 2019 Google Inc.
  Licensed under http://www.apache.org/licenses/LICENSE-2.0 <see LICENSE file>
}}
<div class="nav internav {{lowercase instance.type}}">
<div class="tabs priority {{^if isAuditScope}}single-set{{/if}}">
  <ul class="internav">
    {{#if showTabs}}
      {{#priorityTabs}}
        <inner-nav-item {widget}="." 
          {active-widget}="activeWidget"
          {show-title}="true" 
          {show-all-tabs}="instance.constructor.obj_nav_options.show_all_tabs"
          (close)="closeTab(%event)"></inner-nav-item>
      {{/priorityTabs}}
    {{/if}}

    {{^if isAuditScope}}
      <add-tab-button
        {instance}="instance"
        {widget-list}="widgetList"
        {add-tab-title}="'Add Tab'"
        {has-hidden-widgets}="hasHiddenWidgets">
      </add-tab-button>
    {{/if}}
  </ul>
</div>

{{#if showTabs}}
  {{#if isAuditScope}}
    <div class="tabs not-priority">
      <inner-nav-collapse {title}="'Show Audit Scope'">
        <ul class="internav">
          {{#notPriorityTabs}}
            <inner-nav-item {widget}="."
              {active-widget}="activeWidget" 
              {show-title}="false"
              {show-all-tabs}="instance.constructor.obj_nav_options.show_all_tabs"
              (close)="closeTab(%event)"></inner-nav-item>
          {{/notPriorityTabs}}

          <add-tab-button
            {instance}="instance"
            {widget-list}="widgetList"
            {add-tab-title}="'Add Scope'"
            {has-hidden-widgets}="hasHiddenWidgets">
          </add-tab-button>
        </ul>
      </inner-nav-collapse>
    </div>
  {{/if}}
{{/if}}
</div>
